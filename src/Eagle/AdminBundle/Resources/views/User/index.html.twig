{% extends '::admin_base.html.twig' %}

{% block body -%}
    {#<div style="padding-top: 20px; padding-left: 231px;">
    <h1>User list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Username</th>
                <th>Password</th>
                <th>Isadmin</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_user_show', { 'id': entity.id }) }}">{{ entity.username }}</a></td>
                <td>{{ entity.password }}</td>
                <td>{{ entity.isAdmin }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_user_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_user_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_user_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    </div>      #}

    <style>
        .ui-slider-vertical{
            height: 200px;
            margin-top: 13%;
        }
    </style>


    <!--Body content-->
    <div id="content" class="clearfix">
        <div class="contentwrapper"><!--Content wrapper-->

            <div class="heading">

                <h3>Show Users</h3>                    

                <div class="resBtnSearch">
                    <a href="#"><span class="icon16 brocco-icon-search"></span></a>
                </div>               

            </div><!-- End .heading-->

            <!-- Build page from here: -->

            <div class="row-fluid">
                <div class="span12">
                    <div class="box">

                        <div class="title">

                            <h4>
                                <span class="icon16 brocco-icon-grid"></span>
                                <span>User list</span>
                            </h4>
                            <a href="#" class="minimize">Minimize</a>
                        </div>
                        <div class="content noPad">
                            <table class="responsive table table-bordered">
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>Username</th>
                                        <th>Password</th>
                                        <th>User Role</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for entity in entities %}
                                        <tr>
                                            <td>{{ entity.id }}</td>
                                            <td><a href="{{ path('admin_user_show', { 'id': entity.id }) }}">{{ entity.username }}</a></td>
                                            <td>{{ entity.password }}</td>
                                            <td>{{ (entity.isAdmin == '1') ? 'Admin' : 'Normal User' }}</td>                                            
                                            <td>
                                                <div class="controls center">
                                                    <a href="{{ path('admin_user_edit', { 'id': entity.id }) }}" title="Edit User" class="tip"><span class="icon12 icomoon-icon-pencil"></span></a>
                                                    <a href="{{ path('admin_user_show', { 'id': entity.id }) }}" title="Show User" class="tip"><span class="icon12 brocco-icon-address-book"></span></a>
{#                                                    <a href="#" title="Remove task" class="tip"><span class="icon12 icomoon-icon-remove"></span></a>#}
                                                </div>
                                            </td>
                                        </tr>
                                    {% endfor %}                                       
                                </tbody>
                            </table>

                            <ul style="padding-top:10px;">
                                <li>
                                    <a href="{{ path('admin_user_new') }}">
                                        Create a new entry
                                    </a>
                                </li>
                            </ul>
                        </div>

                    </div><!-- End .box -->
                </div><!-- End .span8 -->           
            </div><!-- End .row-fluid -->





        </div><!-- End contentwrapper -->
    </div><!-- End #content -->

</div><!-- End #wrapper -->



</body>

{% endblock %}
